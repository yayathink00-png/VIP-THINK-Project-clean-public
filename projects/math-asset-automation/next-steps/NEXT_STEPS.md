# Next Steps

1. Replace all platform resource identifiers with placeholders.
2. Create synthetic material task fixtures.
3. Extract sanitized scripts into this clean repo.
4. Add asset review and approval records.
5. Add approved demo assets only after maintainer confirmation.
6. Implement `next` and `validate` to prevent wrong-table or missing-field operation.
7. Implement `prepare-dir` and `upload-dir` to reduce manual file-path mistakes.
8. Implement `export-task` so image-generation instructions can be handed off without exposing platform records.

